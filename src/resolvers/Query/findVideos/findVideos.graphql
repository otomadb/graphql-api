type Query {
  findVideos(
    first: Int
    after: String

    last: Int
    before: String

    orderBy: FindVideosOrderBy! = { createdAt: DESC }
  ): VideoConnection!
}

input FindVideosOrderBy {
  createdAt: SortOrder
}
