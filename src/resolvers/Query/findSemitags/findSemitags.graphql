type Query {
  findSemitags(
    first: Int
    after: String

    last: Int
    before: String

    orderBy: FindSemitagsOrderBy! = { createdAt: DESC }

    expect: [ID!]! = [] @deprecated

    checked: Boolean
  ): SemitagConnection!
}

input FindSemitagsOrderBy {
  updatedAt: SortOrder
  createdAt: SortOrder
}
