type Mutation {
  signout: SignoutPayload!
}

union SignoutPayload = SignoutSucceededPayload | SignoutFailedPayload

type SignoutSucceededPayload {
  session: Session!
}

type SignoutFailedPayload {
  message: SignoutFailedMessage!
}

enum SignoutFailedMessage {
  "セッションIDが取得できない"
  NO_SESSION_ID

  "セッションが存在しない"
  SESSION_NOT_FOUND

  "原因不明のエラー"
  UNKNOWN
}
