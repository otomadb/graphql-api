type Mutation {
  likeVideo(input: LikeVideoInput!): LikeVideoPayload!
}

input LikeVideoInput {
  videoId: ID!
}

union LikeVideoPayload = LikeVideoSuccessedPayload | LikeVideoFailedPayload

type LikeVideoSuccessedPayload {
  registration: MylistRegistration!
}

type LikeVideoFailedPayload {
  message: LikeVideoFailedMessage!
}

enum LikeVideoFailedMessage {
  FORBIDDEN

  INVALID_VIDEO_ID

  VIDEO_NOT_FOUND
  VIDEO_ALREADY_LIKED

  UNKNOWN
}
