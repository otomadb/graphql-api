interface VideoEvent {
  id: ID!
  createdAt: DateTime!
  video: Video!
  user: User!
}

type VideoEventsConnection {
  nodes: [VideoEvent!]!
}

"Videoを追加した際に発生．（初回のみ）"
type VideoRegisterEvent implements VideoEvent {
  id: ID!
  createdAt: DateTime!
  user: User!
  video: Video!
}
