type Tag {
  children(
    first: Int
    after: String

    last: Int
    before: String

    orderBy: TagChildrenOrderBy! = { createdAt: DESC }
  ): TagParentConnection!
}

input TagChildrenOrderBy {
  createdAt: SortOrder
}
