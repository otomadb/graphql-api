type Tag {
  parents(
    first: Int
    after: String

    last: Int
    before: String

    orderBy: TagParentsOrderBy! = { createdAt: DESC }

    categoryTag: Boolean
  ): TagParentConnection!
}

input TagParentsOrderBy {
  createdAt: SortOrder
}
